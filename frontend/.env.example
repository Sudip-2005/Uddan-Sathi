# Environment Variables for UdaanSathi Frontend

# ⚠️ IMPORTANT: Vite requires all environment variables to start with VITE_
# Variables without this prefix will NOT be available in your app

# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
VITE_CLERK_SIGN_IN_FORCE_REDIRECT_URL=/user/dashboard
VITE_CLERK_SIGN_UP_FORCE_REDIRECT_URL=/user/dashboard

# Backend API URL
# For local development, use: http://localhost:5000
# For production, use your Render URL: https://your-app.onrender.com
VITE_BACKEND_URL=http://localhost:5000

# API Keys
VITE_TRAIN_API_KEY=your_train_api_key_here
VITE_GEOAPIFY_KEY=your_geoapify_key_here
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# HOW TO USE THIS FILE:
# =============================================================================
# 
# 1. For LOCAL DEVELOPMENT:
#    - Copy this file to .env.local
#    - Fill in your actual API keys
#    - Set VITE_BACKEND_URL=http://localhost:5000
#
# 2. For VERCEL DEPLOYMENT:
#    - Go to your Vercel project dashboard
#    - Navigate to: Settings → Environment Variables
#    - Add each variable above with your production values
#    - Set VITE_BACKEND_URL to your Render backend URL
#    - Example: VITE_BACKEND_URL=https://uddan-sathi.onrender.com
#
# 3. IMPORTANT NOTES:
#    - NEVER commit .env.local to git (it's in .gitignore)
#    - All variables MUST start with VITE_ to be accessible
#    - After changing .env.local, restart your dev server
#    - Vercel will automatically use these variables during build
#
# =============================================================================
